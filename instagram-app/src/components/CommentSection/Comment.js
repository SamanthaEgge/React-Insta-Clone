import React from 'react';

import './CommentSection.css'

const Comment = (props, index) => {
    console.log(props)
    return (
        <div>
        
        </div>
    )
}

export default Comment;